import 'package:flutter/material.dart';

class ProductImageError extends StatelessWidget {
  const ProductImageError({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
